{
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp -r model dist/model && cp fake-jquery.js dist/fake-jquery.js",
    "build-lib": "vite build -c vite.config.esm.js"
  },
  "devDependencies": {
    "vite": "^4.0.4"
  }
}
