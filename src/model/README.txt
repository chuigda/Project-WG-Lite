Models are not directly stored in this git repository. Instead, you need to
get models from https://github.com/chuigda/Project-WG  and convert the .mesh
files to JSON format using convert-all.sh (bash and Node.js required).
